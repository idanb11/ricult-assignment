const express = require('express');

const router = express.Router();

/** GET /api/v1/look/<influencerId>/<lookId> - return a single look. */
// router.route('/:influencerId/:lookId').get(lookCtrl.getLook);


module.exports = router;
